# Use pkgx to ensure uv is available and synchronize the Python environment.
# This command installs/updates dependencies defined in pyproject.toml or requirements.txt
# and manages the Python interpreter.
echo "✨ Ensuring uv is available and synchronizing Python environment with pkgx..."
pkgx uv sync --all-extras --inexact --managed-python
echo "✅ Python environment synchronized."

# Activate the virtual environment created by uv.
# This makes the Python interpreter and installed packages available in the shell.
echo "🚀 Activating virtual environment..."
. .venv/bin/activate
echo "🟢 Virtual environment activated."